sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pparidadocker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pparidadocker/docker-react npm run test
